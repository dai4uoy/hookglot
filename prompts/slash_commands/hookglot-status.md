---
description: Show current hookglot configuration
allowed-tools: Bash
---

Use the Bash tool to run:

```
hookglot status
```

The bash output is automatically shown to the user by Claude Code. Do NOT repeat
or summarize it.

Your text response must be EXACTLY this and nothing more:

```
[hg-bypass]
```

That's it. One line. No commentary.
