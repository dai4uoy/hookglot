---
description: Switch hookglot method (1 = input-only, 2 = output-only, off = disable)
argument-hint: <1 | 2 | off>
allowed-tools: Bash
---

Use the Bash tool to run this command:

```
hookglot switch $ARGUMENTS
```

The bash output is automatically displayed by Claude Code. Do NOT repeat or echo it.

Your text response must be EXACTLY this and nothing more:

```
[hg-bypass]

Type /clear to apply.
```

Do not summarize, explain, or echo the bash output. Just the marker line and the
/clear reminder.
