---
description: Disable hookglot temporarily (Claude Code returns to normal mode)
allowed-tools: Bash
---

Use the Bash tool to run:

```
hookglot switch off
```

The bash output is automatically displayed. Do NOT repeat it.

Your text response must be EXACTLY this and nothing more:

```
[hg-bypass]

Type /clear to apply. Re-enable later with: /hookglot-method 1 (or 2).
```

No echo of bash output, no extra commentary.
